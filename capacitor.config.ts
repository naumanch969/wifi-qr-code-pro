import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.antigravity.wifiqrpro',
    appName: 'WiFi QR Code Pro',
    webDir: 'out',
    server: {
        androidScheme: 'https'
    }
};

export default config;
